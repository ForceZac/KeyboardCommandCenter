// @kcc/core — shared types and business logic
// Importable by packages/web, packages/desktop, and packages/overlay
// No framework dependencies allowed in this package

// Re-export all types as they are defined
export * from './types';

# 2026-05-11

## Reviewer run — 02:18 ET

### PR #26 — TASK-0026 Desktop Panel Favorites (round 2)

**Decision:** Changes requested (round 3 needed)

**Issue 1 (showFallback view-awareness) — FIXED:**
`index.ts` lines 181–193 guard on `currentView === 'favorites'` correctly.
When in favorites view: fallback content updates silently, search bar stays visible, fallback container stays hidden.

**Issue 2 (index.ts integration test) — STILL OPEN:**
Developer added `favorites-list.test.ts` (unit tests for renderer module — thorough, 12 tests) and updated `search.test.ts` for both views. But the `index.ts` controller wiring (tab switching, delegated fav-btn, showFallback view-awareness) has zero test coverage.

Also: developer's PR notes claimed "favorites-integration.test.ts (26 tests), 345 total" — that file does not exist; actual suite is 319 tests across 13 files.

**For round 3:** Add 3 tests to `fallback-integration.test.ts` or a new `index-integration.test.ts`:
1. `showFallback()` in favorites view — fallback stays hidden, search stays visible
2. Tab click "My Favorites" — `#shortcuts-container` hides, `#favorites-container` shows
3. Delegated `.fav-btn` click in `#shortcuts-container` — `.favorited` class toggles

Review comment posted: https://github.com/ForceZac/KeyboardCommandCenter/pull/26#issuecomment-4418034618

## 2026-05-11 02:55 ET TRD-WATCHER
- did: reviewed TRD for TASK-0027 (re-review after changes-requested round)
- decision: approved
- key finding: appId nullable fix confirmed correct — schema now explicitly marks appId as nullable FK with per-type semantics; architecture otherwise unchanged from prior review and remains sound
- metrics: task=TASK-27 | decision=approved
